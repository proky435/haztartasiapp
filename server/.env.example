# Háztartási App - Környezeti változók példa
# Másold át .env fájlba és állítsd be a valós értékeket

# =====================================================
# ADATBÁZIS BEÁLLÍTÁSOK
# =====================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=haztartasi_app
DB_USER=app_user
DB_PASSWORD=secure_password123
DB_SSL=false

# Teljes connection string (alternatíva)
DATABASE_URL=postgresql://app_user:secure_password123@localhost:5432/haztartasi_app

# =====================================================
# REDIS BEÁLLÍTÁSOK
# =====================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Redis connection string (alternatíva)
REDIS_URL=redis://localhost:6379

# =====================================================
# ALKALMAZÁS BEÁLLÍTÁSOK
# =====================================================
NODE_ENV=development
PORT=3001
API_VERSION=v1

# JWT titkok
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-too
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Session titkos kulcs
SESSION_SECRET=your-super-secret-session-key-change-this

# =====================================================
# KÜLSŐ API-K
# =====================================================

# Open Food Facts API
OPENFOODFACTS_API_URL=https://world.openfoodfacts.org
OPENFOODFACTS_USER_AGENT=HaztartasiApp/1.0

# Email szolgáltatás (SendGrid példa)
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@haztartasi.app
FROM_NAME=Háztartási App

# SMS szolgáltatás (Twilio példa)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Push értesítések (Firebase példa)
FIREBASE_PROJECT_ID=your-firebase-project
FIREBASE_PRIVATE_KEY=your-firebase-private-key
FIREBASE_CLIENT_EMAIL=your-firebase-client-email

# =====================================================
# FÁJL FELTÖLTÉS
# =====================================================

# Helyi tárolás
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# AWS S3 (opcionális)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=eu-central-1
AWS_S3_BUCKET=haztartasi-app-uploads

# =====================================================
# BIZTONSÁGI BEÁLLÍTÁSOK
# =====================================================

# CORS beállítások
CORS_ORIGIN=http://localhost:3000,https://haztartasi.app
CORS_CREDENTIALS=true

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Bcrypt rounds
BCRYPT_ROUNDS=12

# =====================================================
# LOGGING ÉS MONITORING
# =====================================================

# Log szint (error, warn, info, debug)
LOG_LEVEL=info
LOG_FORMAT=combined

# Sentry (hibakezelés)
SENTRY_DSN=your-sentry-dsn

# =====================================================
# FEJLESZTŐI BEÁLLÍTÁSOK
# =====================================================

# Debug mód
DEBUG=haztartasi:*

# Automatikus adatbázis migráció
AUTO_MIGRATE=true

# Seed adatok betöltése
LOAD_SAMPLE_DATA=true

# API dokumentáció engedélyezése
ENABLE_API_DOCS=true

# Cron Scheduler engedélyezése (értesítések, lejárati figyelmeztetések)
ENABLE_CRON_SCHEDULER=false

# Automatikus törlés - hány nap után töröljük a lejárt termékeket
AUTO_DELETE_DAYS_AFTER_EXPIRY=3
